NEXT_PUBLIC_DEFAULT_SITE_URL=http://localhost:3000

# Vercel Analytics: set to "true" to enable (script + custom events). Omit or any other value = disabled.
# NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Required: old site's dedicated origin, for example a separate Vercel domain or legacy subdomain.
LEGACY_SITE_ORIGIN=https://www.unkey.com
